@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Личные данные";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<div class="d-flex mb-4">
    <h3 class="text-primary fw-bold">@ViewData["Title"]</h3>
</div>
<div class="row">
    <div class="col-md-6">
        <p>Ваш аккаунт содержит личные данные, которые вы нам предоставили. Эта страница позволяет скачать или удалить эти данные.</p>
        <p>
            <strong>Удаление этих данных навсегда удалит ваш аккаунт, и это не может быть восстановлено.</strong>
        </p>
        <div class="d-flex">
            <form id="download-data" asp-page="DownloadPersonalData" method="post" class="me-2">
                <button class="btn btn-primary shadow" type="submit">Скачать</button>
            </form>
            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger shadow">Удалить</a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
